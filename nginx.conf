server {
  listen 3005;
  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    # serve index.html for any 404 to enable client-side routing
    try_files $uri $uri/ /index.html;
  }
}
